# Draft projects

Folders here are not published by `generate.py`.

When a project is ready, move it directly under `projects/` without changing its
safe name, for example from `_drafts/0061-commandmoment/` to
`projects/0061-commandmoment/`.
