# Draft projects

Folders here are not published by `generate.py`.

When a project is ready, move it to a direct numeric folder under `projects/`,
for example from `_drafts/0061-commandmoment/` to `0061/`.

