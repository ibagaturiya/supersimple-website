# Archive

Legacy files retained for reference. Nothing in this directory is required by
the live website.

- `legacy-styles/`: unused historical CSS.
- `generated-pages/`: stale generated HTML retained instead of deleted.
